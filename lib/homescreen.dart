import 'package:flutter/material.dart';
class PorrtfolioClass extends StatelessWidget {
  const PorrtfolioClass({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:Expanded(
          flex:50,
          child: Column(
        children: [

        ],
      ))
    );
  }
}
